# generated_by: forge:architect-domain-extraction
# generated_at: 2026-08-27
# tier: medium
commands:
  - id: CMD-001
    name: GenerateRandomGrid
    actor: ACT-001
    target_aggregate: AGG-001
    preconditions: []
    produces: [EVT-001]
    source: [US-001, US-008]
  - id: CMD-002
    name: GenerateGridFromLibraryImage
    actor: ACT-001
    target_aggregate: AGG-001
    preconditions: []
    produces: [EVT-002]
    source: [US-002, US-008]
  - id: CMD-003
    name: ConvertUploadedImage
    actor: ACT-001
    target_aggregate: AGG-001
    preconditions: []
    produces: [EVT-003]
    source: [US-003, US-008]
  - id: CMD-004
    name: ComputeClues
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: []
    produces: [EVT-004]
    source: [US-004]
  - id: CMD-005
    # NOTE 2026-09-12 (FR-024): the verdict this command produces now also
    # carries the first-fixed-point undecided mask and, on MANY, a second
    # witness solution — see EVT-006's payload. Count semantics unchanged.
    name: CheckUniqueness
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-001]
    produces: [EVT-005, EVT-006]
    source: [US-005]
  - id: CMD-006
    # NOTE 2026-09-12 (FR-025, DEC pending): for random mode this command's
    # redraw is one alternative; CMD-014 (repair) is the other. Library mode
    # keeps this command under every alternative.
    name: RegeneratePuzzle
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-003]
    produces: [EVT-007]
    source: [US-006]
  - id: CMD-007
    # NOTE 2026-09-12 (FR-013 amendment, mechanism half): the cells this
    # command flips are chosen from EVT-006's undecided/disagreement mask,
    # nearest the ink boundary first — no longer from the 2x2 switching-block
    # ranking. Cap, cumulative-from-original property and EVT-008 unchanged.
    name: AdjustImagePixels
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-003]
    produces: [EVT-008]
    source: [US-007]
  - id: CMD-008
    name: ReportImageConversionFailure
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-003]
    produces: [EVT-009]
    source: [US-007]
  - id: CMD-009
    name: ScoreDifficulty
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-002]
    produces: [EVT-010]
    source: [US-008]
  - id: CMD-010
    name: ResamplePuzzleForDifficulty
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-003]
    produces: [EVT-011]
    source: [US-008]
  - id: CMD-011
    name: AbandonGeneration
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-003]
    produces: [EVT-012]
    source: [US-006, US-007, US-008]
  - id: CMD-012
    name: ExportPuzzleAsImage
    actor: ACT-001
    target_aggregate: AGG-001
    preconditions: [INV-002]
    produces: [EVT-013]
    source: [US-009]
  - id: CMD-013
    name: ExportPuzzleAsData
    actor: ACT-001
    target_aggregate: AGG-001
    preconditions: [INV-002]
    produces: [EVT-014]
    source: [US-010]
  - id: CMD-014
    # New 2026-09-12 (FR-025 / POL-006, DEC pending): repair a non-unique
    # random-mode candidate in place — flip one filled/empty pair inside the
    # undecided/disagreement region EVT-006 carries (FR-024), keeping the
    # filled-cell count exact — then hand the result back to CMD-005. Counts
    # against the same INV-003 counter as CMD-006. Whether it is ever issued
    # is the open decision; it is modeled so either outcome fits.
    name: RepairCandidateGrid
    actor: ACT-002
    target_aggregate: AGG-001
    preconditions: [INV-003]
    produces: [EVT-015]
    source: [US-006]
  # New 2026-09-22 (delta — book generator, FR-030..FR-039). The first
  # commands on AGG-002 Book. Existing admin-panel book operations
  # (create, reorder, retitle, cover, delete) are not modelled here: only
  # those the 2026-09-22 requirements put new rules on.
  - id: CMD-015
    # Print setup: trim, margins and the distribution plan (general plan +
    # per-bucket cells, with a hand-edited marker per cell).
    name: SaveBookPrintSetup
    actor: ACT-001
    target_aggregate: AGG-002
    preconditions: [INV-005]
    produces: [EVT-016]
    source: [US-023, US-025]
  - id: CMD-016
    # Both add routes (selection step, detail page paste-IDs form). Carries
    # the per-puzzle below-floor overrides and, for a published book, the
    # confirmation.
    # AMENDED 2026-09-22 (b): INV-009 added — an added puzzle is placed
    # inside its tier's level (FR-041).
    # AMENDED 2026-09-22 (c): INV-012 added — an add on a book that has left
    # draft returns it to draft in the same change (FR-038, ADR-0035
    # clarification).
    name: AddPuzzlesToBook
    actor: ACT-001
    target_aggregate: AGG-002
    preconditions: [INV-006, INV-008, INV-009, INV-012]
    produces: [EVT-017]
    source: [US-023, US-026]
  - id: CMD-017
    # AMENDED 2026-09-22 (c): INV-012 added — a removal on a book that has
    # left draft returns it to draft in the same change (FR-038).
    name: RemovePuzzleFromBook
    actor: ACT-001
    target_aggregate: AGG-002
    preconditions: [INV-008, INV-012]
    produces: [EVT-018]
    source: [US-027]
  - id: CMD-018
    name: MarkBookReady
    actor: ACT-001
    target_aggregate: AGG-002
    preconditions: [INV-007]
    produces: [EVT-019]
    source: [US-026]
  - id: CMD-019
    # Existing (Finalise / download-pdf / generate-pdf routes); modelled now
    # because FR-030..FR-033 change what it produces.
    # AMENDED 2026-09-22 (b): the PDF's page composition obeys INV-010
    # (two-up pages, FR-040) and prints in INV-009's order with a divider
    # page per non-empty level (FR-041).
    # AMENDED 2026-09-22 (c): the answer key obeys INV-011 (6-up / 4-up
    # answer pages in puzzle-number order, FR-042), and every page is laid
    # out with mirrored margins by page parity (FR-030 / FR-032 amended).
    name: GenerateBookPdf
    actor: ACT-001
    target_aggregate: AGG-002
    preconditions: [INV-009, INV-010, INV-011]
    produces: [EVT-020]
    source: [US-023, US-024]
  - id: CMD-020
    # Issued by POL-007 only.
    name: RederiveBookPlanMatrix
    actor: ACT-002
    target_aggregate: AGG-002
    preconditions: [INV-005]
    produces: [EVT-021]
    source: [US-025]
  - id: CMD-021
    # BK-UI-9 "general info" step on an existing book (today only creation
    # sets these fields).
    name: EditBookGeneralInfo
    actor: ACT-001
    target_aggregate: AGG-002
    preconditions: []
    produces: [EVT-022]
    source: [US-027]
  # New 2026-09-22 (b) (delta — book layout, FR-041). The arrangement step's
  # reorder (move up / move down / reorder, BookManager.reorder_puzzles and
  # move_puzzle_up/down) existed but was left unmodelled by the first
  # delta, having no rule on it; FR-041 now confines a move to its level.
  - id: CMD-022
    name: ReorderBookPuzzles
    actor: ACT-001
    target_aggregate: AGG-002
    preconditions: [INV-009]
    produces: [EVT-023]
    source: [US-024, US-027]
