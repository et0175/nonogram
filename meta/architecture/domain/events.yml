# generated_by: forge:architect-domain-extraction
# generated_at: 2026-08-27
# tier: medium
events:
  - id: EVT-001
    name: RandomGridGenerated
    triggered_by: CMD-001
    aggregate: AGG-001
    source: [US-001]
    payload_summary: size, density, grid
  - id: EVT-002
    name: LibraryGridGenerated
    triggered_by: CMD-002
    aggregate: AGG-001
    source: [US-002]
    payload_summary: library_key, size, grid
  - id: EVT-003
    name: UploadedImageConverted
    triggered_by: CMD-003
    aggregate: AGG-001
    source: [US-003]
    payload_summary: source_image_path, size, grid
  - id: EVT-004
    name: CluesComputed
    triggered_by: CMD-004
    aggregate: AGG-001
    source: [US-004]
    payload_summary: row_clues, column_clues
  - id: EVT-005
    name: PuzzleUniquenessConfirmed
    triggered_by: CMD-005
    aggregate: AGG-001
    source: [US-005]
    payload_summary: solution_count=1
  - id: EVT-006
    # AMENDED 2026-09-12 (FR-024): payload gains the first-fixed-point
    # undecided mask (grid-shaped, list[list[bool]] — ADR-0012, never a
    # bitmask) and, when solution_count is MANY, a second witness solution.
    # POL-002 (nudge) and POL-006 (repair) read the ambiguity off this
    # payload instead of guessing at it. Name, trigger and count unchanged.
    name: PuzzleUniquenessCheckFailed
    triggered_by: CMD-005
    aggregate: AGG-001
    source: [US-005, US-006, US-007]
    payload_summary: solution_count (0 or >1), mode, undecided_mask (cells open at the first fixed point), second_witness (only when MANY)
  - id: EVT-007
    name: PuzzleRegenerated
    triggered_by: CMD-006
    aggregate: AGG-001
    source: [US-006]
    payload_summary: retry_count, new_grid
  - id: EVT-008
    name: ImagePixelsAdjusted
    triggered_by: CMD-007
    aggregate: AGG-001
    source: [US-007]
    payload_summary: nudge_count, cells_flipped, new_grid
  - id: EVT-009
    name: ImageConversionFailed
    triggered_by: CMD-008
    aggregate: AGG-001
    source: [US-007]
    payload_summary: nudge_count_at_cap, reason
  - id: EVT-010
    # AMENDED 2026-09-12: solver_time LEAVES the scoring signals (NFR-007/
    # CON-014 — it may still be telemetry, it is never a score input).
    # FR-026 (DEC pending) adds line-reasoning depth signals (fixed-point
    # sweeps, first-sweep share, lines needing the full placement DP) and an
    # explicit requires_guessing attribute — flag or separate tier is the
    # open decision. Name and trigger unchanged.
    # AMENDED 2026-09-12 (b) (FR-029): the scored result also carries the
    # ORDERED strategies list derived from the same verifying solve (no
    # re-solve) — a pure function of the clues (NFR-007). Enum members are
    # provisional, fixed by DEC-030/DEC-031. Payload amendment only; no new
    # event or command.
    name: DifficultyScored
    triggered_by: CMD-009
    aggregate: AGG-001
    source: [US-008]
    payload_summary: score, requires_guessing (flag or tier — DEC pending, FR-026), signals(line_logic_coverage, backtracking_amount, size, clue_density, fixed_point_sweeps, first_sweep_share, full_dp_lines), strategies (ordered list from the fixed enum — FR-029); solver_time is telemetry only, never a score input (NFR-007)
  - id: EVT-011
    name: PuzzleResampledForDifficulty
    triggered_by: CMD-010
    aggregate: AGG-001
    source: [US-008]
    payload_summary: retry_count, requested_tier, new_grid
  - id: EVT-012
    name: GenerationAbandoned
    triggered_by: CMD-011
    aggregate: AGG-001
    source: [US-006, US-007, US-008]
    payload_summary: reason(uniqueness|difficulty|nudge_cap), retry_count_at_abandon
  - id: EVT-013
    name: PuzzleExportedAsImage
    triggered_by: CMD-012
    aggregate: AGG-001
    source: [US-009]
    payload_summary: format(png|svg), file_path
  - id: EVT-014
    # AMENDED 2026-09-12 (b) (FR-029): the JSON payload written here carries
    # a `strategies` array equal to the puzzle's ordered list (AC-140).
    name: PuzzleExportedAsData
    triggered_by: CMD-013
    aggregate: AGG-001
    source: [US-010]
    payload_summary: format(json|csv), file_path
  - id: EVT-015
    # New 2026-09-12 (FR-025 / POL-006 / CMD-014, DEC pending): a non-unique
    # random-mode candidate was repaired in place — one filled/empty pair
    # flipped inside the undecided/disagreement region, filled count exact.
    # Always followed by a fresh CMD-005 on the repaired grid (INV-002).
    name: CandidateGridRepaired
    triggered_by: CMD-014
    aggregate: AGG-001
    source: [US-006]
    payload_summary: retry_count, flipped_pair (one filled cell, one empty cell — both inside undecided_mask), repaired_grid
