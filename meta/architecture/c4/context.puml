@startuml
!include <C4/C4_Context>
' ============================================================
' C4 Context Diagram — Auto-generated by forge:architect-synthesis.
' Do not edit manually.
' Source: meta/architecture/domain/ + decisions/
' Generated from:
'   ACT-001 (PuzzleCreator, human)
'   ACT-002 (System — the tool's own automation: solver, scorer, retry
'            loops; SAME local process, therefore NOT drawn as an actor
'            or external system; it appears as internal components at L3)
'   external_systems.yml — EMPTY, and still correct after CARD-112
'            (2026-09-22): no third-party or sibling-owned system. The
'            admin panel's Postgres database is NOT an EXT-XXX — it is the
'            system's own store, owned and migrated by this repository,
'            so it is a ContainerDb inside the boundary (containers.puml),
'            not an external system beside it. Constraints: CON-007 and
'            CON-017, which superseded CON-001 and CON-003.
'   The local filesystem is drawn below as a boundary element for
'            readability only — per external_systems.yml it is
'            infrastructure, NOT an EXT-XXX.
' DELTA 2026-09-22 (book generator, FR-030..FR-041): no actor or external
'   system added — Amazon KDP is a print specification the book PDF meets
'   (CON-018), not an integration; ADR-0033 keeps Book in this system's one
'   context. Only the admin relationship's label changes.
' ADR references:
'   ADR-0008 (installable package, `nonogram` console entry point)
'   ADR-0010 (stdlib argparse CLI surface)
'   ADR-0015 (--seed flag; seed echoed to the user for reproducibility)
' ============================================================
title System Context — Nonogram Generator

Person(puzzleCreator, "Puzzle Creator", "ACT-001: developer/user running the CLI. Chooses mode (random / library / image), size, density, difficulty tier and seed; triggers export.")

System(nonogram, "Nonogram Generator", "A generation tool — CLI and loopback web UI (ADR-0008, ADR-0019) — that sources a solution grid, derives clues, guarantees exactly one logical solution, calibrates difficulty and exports print-ready or structured files; and an admin panel, deployed separately, that turns those puzzles into books.")

System_Ext(filesystem, "Local Filesystem", "Infrastructure, not an EXT-XXX: source images in, PNG/SVG/JSON/CSV exports out (--out path).")

Rel(puzzleCreator, nonogram, "Runs `nonogram` with mode, size, density, difficulty, --seed", "CLI / argparse (ADR-0010)")
Rel(nonogram, puzzleCreator, "Prints seed, nudge count (FR-014), and failure reasons", "stdout/stderr")
Rel(puzzleCreator, nonogram, "Reviews batches, curates puzzles, plans and assembles books, and downloads trim-sized book PDFs in the admin panel", "HTTP")
Rel(nonogram, filesystem, "Reads uploaded source image (FR-003); writes PNG/SVG/JSON/CSV exports (FR-011, FR-012)", "local file I/O")

SHOW_LEGEND()
@enduml
