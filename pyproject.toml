[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nonogram-web"
version = "0.1.0"
description = "Nonogram puzzle generator web interface"
requires-python = ">=3.9"

[tool.poetry]
python = "^3.9"
