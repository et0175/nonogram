{
  "name": "nonogram-app",
  "version": "0.1.0",
  "description": "Nonogram puzzle generator with Next.js frontend and Python backend",
  "private": true,
  "scripts": {
    "dev": "cd nonogram-web && npm run dev",
    "build": "cd nonogram-web && npm install && npm run build",
    "start": "cd nonogram-web && npm start",
    "postinstall": "pip install -e . && cd nonogram-web && npm install"
  }
}

