I'd like to update the batch generation flow.
1. Remove Random generation (left «From images» mode only)
2. Source directory - choose directory to select all images from that directory
3. Size field - 3 options:
- number - (10-30 the same for all puzzles) all puzzles will take this number as «size» parameter 
- min - generate with min possible size;
- max - generate each nonogram with the max possible size for puzzles to look good
3. Add new page - preview of the selected images - for the user to be able to preview each image 
This picture will contain -  list of small cropped pictures  where 
the user can update size for the individual puzzle (would be nice to have 3 options) - this functionality has already been implemented in src\nonogram\web 
puzzle_name field(the same as picture file name by default), we'll add a possibility to filter by puzzle_name (later))
Add\remove pictures to the batch  functionality
4. Puzzles preview page - please add be possibility to see the picture (solution of the puzzle) 

